rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Bachelor Point Hostel Management System security rules
    // Allow read, write, and list operations to any authenticated user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
